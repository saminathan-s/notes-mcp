name: ascii-workflow
on: push
jobs:
  create-ascii-art:
    runs-on: ubuntu-latest
    steps:
      - name: List directory
        run: ls -a
      - name: Execute script
        run: |
          chmod +x ascii.sh
          ./ascii.sh
